[pytest]
# ➊ Add the repo root (“.”) to sys.path so `import Domain.…` works everywhere
pythonpath = .

# ➋ Tell pytest where your test packages live (optional but nice)
testpaths =
    Tests/UnitTests

# ➌ Extra default flags you like (optional)
addopts = -ra